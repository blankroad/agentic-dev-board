# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.env

# agentboard runtime state (T4 rename 2026-04-23: .devboard/ → .agentboard/;
# legacy patterns kept for repos that have not migrated yet)
.agentboard/runs/
.agentboard/state.json
.agentboard/state.json.lock
.agentboard/goals/
.agentboard/retros/
.agentboard/learnings/
.agentboard/activity.jsonl
.agentboard/.iron-law-history.jsonl
.agentboard/mcp_calls.jsonl
.devboard/runs/
.devboard/state.json
.devboard/state.json.lock
.devboard/goals/
.devboard/retros/
.devboard/learnings/
.devboard/activity.jsonl
.devboard/.iron-law-history.jsonl
.devboard/mcp_calls.jsonl

# agentboard TUI snapshots (Layer 1/2/3 capture artifacts — regenerated per run)
tui_snapshots/

# agentboard config is tracked (intentional)
!.agentboard/config.yaml
!.devboard/config.yaml

# Testing
.pytest_cache/
.coverage
htmlcov/

# Editors
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code — local-only files
.claude/settings.local.json
.claude/hooks/
.claude/skills/

# OpenCode — local-only files (install artifacts mirror skills/)
.opencode/skills/

# Agent host local configs (contain absolute paths)
.mcp.json
opencode.json
