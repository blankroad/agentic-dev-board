# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.env

# devboard runtime state
.devboard/runs/
.devboard/state.json
.devboard/state.json.lock
.devboard/goals/
.devboard/retros/
.devboard/learnings/
.devboard/activity.jsonl
.devboard/.iron-law-history.jsonl

# devboard TUI snapshots (Layer 1/2/3 capture artifacts — regenerated per run)
tui_snapshots/

# devboard config is tracked (intentional)
!.devboard/config.yaml

# Testing
.pytest_cache/
.coverage
htmlcov/

# Editors
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code — local-only files
.claude/settings.local.json
.claude/hooks/
.claude/skills/

# OpenCode — local-only files (install artifacts mirror skills/)
.opencode/skills/

# Agent host local configs (contain absolute paths)
.mcp.json
opencode.json
